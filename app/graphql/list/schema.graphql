type weatherJob {
  id: ID!
  cityName: String,
  remark: String,
  users: [String]
}

type JobList {
  count: Int,
  items: [weatherJob]
}

type Query {
  weatherJobs(limit: Int, offset: Int): JobList,
  hello: String
}

type deleteResult {
  ok: Int,
  n: Int
}

type Mutation {
  createJob(
    remark: String!
    cityName: String!
    users: [String]!
  ): weatherJob,
  deleteJob(id: ID!): weatherJob,
  deleteAllJobs: deleteResult,
}